// Fix ticker to show REAL market prices instead of simulator random walk

// BEFORE (line ~608):
const tickerItems = useMemo(() => {
  return SYMS.map(sym => {
    const price = prices[sym][prices[sym].length - 1];
    const prevPrice = prices[sym][prices[sym].length - 2] || price;
    return {
      key: sym,
      name: sym,
      price: price,
      change: ((price - prevPrice) / prevPrice) * 100
    };
  });
}, [prices]);

// AFTER (use live stock data):
const tickerItems = useMemo(() => {
  // Build ticker from live stock data, not simulator prices
  const liveStocks = liveStocksRef.current || stocks || {};
  
  return SYMS.map(sym => {
    const stockData = liveStocks[sym];
    
    if (stockData) {
      // Use live market data
      return {
        key: sym,
        name: sym,
        price: stockData.price || 0,
        change: stockData.changePercent || 0
      };
    } else {
      // Fallback to simulator price (for assets not in stocks API)
      const price = prices[sym]?.[prices[sym].length - 1] || 0;
      const prevPrice = prices[sym]?.[prices[sym].length - 2] || price;
      return {
        key: sym,
        name: sym,
        price: price,
        change: ((price - prevPrice) / prevPrice) * 100
      };
    }
  });
}, [stocks, prices]); // Add stocks dependency
